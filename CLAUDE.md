# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Development workflow: superpowers, always

All development in this repo uses the **superpowers** skills. This is not optional:

- **Before any feature/design work:** `superpowers:brainstorming`
- **Before writing code for a multi-step task:** `superpowers:writing-plans` — every phase gets its own executable plan in `docs/superpowers/plans/` before implementation starts
- **Implementing:** `superpowers:executing-plans` or `superpowers:subagent-driven-development`, with `superpowers:test-driven-development` (RED → GREEN → refactor; never write implementation before a failing test)
- **Any bug or unexpected behavior:** `superpowers:systematic-debugging` before proposing fixes
- **Before claiming anything works:** `superpowers:verification-before-completion` — run the commands, show the output

The user's cadence: review each plan step, implement, test, debug, verify exit criteria, then move to the next step. One phase at a time; do not start a phase whose executable plan hasn't been written and reviewed.

## What this project is

A **retrieval-only knowledge-base MCP server**: hybrid retrieval (BM25 + dense + RRF fusion → graph expansion → cross-encoder rerank → not-found gate) over a deliberately hard, mixed-domain corpus, exposed as three MCP tools (`search`, `fetch_document`, `list_documents`) over stdio. The client LLM synthesizes answers; the server returns cited evidence chunks and an explicit `not_found` signal.

**Read these before doing anything:**
- `docs/PLAN.md` — high-level goals, non-goals, success criteria
- `docs/superpowers/plans/2026-07-09-kb-mcp-server-master-plan.md` — the master technical plan: architecture, interfaces, port surface map, tier taxonomy, per-phase contracts and exit criteria

> **Note:** `docs/superpowers/` is gitignored — plans exist only on the primary dev machine. If you are on a fresh clone and the master plan is missing, stop and ask the user for it; do not improvise architecture from this file alone.

## Locked decisions — do not relitigate

1. **Retrieval-only.** No generation, no answer verification on the server. Client LLM does synthesis.  
2. **RAG code is reference, not a copy source (amended 2026-07-15).** `C:\Users\aniru\RAG` is read for inspiration and technique — never transcribed verbatim. The user considers the old implementation functional but not optimal, and wants to deliberately enhance each module rather than port it as-is. **HARD STOP:** before invoking `superpowers:writing-plans` or writing any implementation code for a module listed in the master plan's §2 "Port Surface Map," stop, explicitly tell the user "this module needs an enhancement-planning pass before I proceed" naming the module, invoke `superpowers:brainstorming`, and wait for the user's actual input in the conversation. Do not treat reading the RAG source yourself as satisfying this — the pass is not complete until the user has weighed in. This supersedes the original "copy + adapt" framing; §2's category labels ("Copy near-verbatim" etc.) now describe what the old code does, not an instruction to transcribe it.
3. **No Ollama anywhere.** Runtime = pip install only, via sentence-transformers. **Models modernized 2026-07-23 (with user — "don't build a 2022 system"):** embedder `Qwen/Qwen3-Embedding-0.6B` (Apache-2.0, 0.6B, 1024-dim via Matryoshka so the Qdrant collection dimension is unchanged; 4B/8B are config-gated GPU upgrades), reranker `BAAI/bge-reranker-v2-m3` (Apache-2.0, 0.6B cross-encoder). Supersedes the original `Snowflake/snowflake-arctic-embed-l-v2.0` (Dec 2024) + `cross-encoder/ms-marco-MiniLM-L-6-v2` (2020) picks — the arctic "same weights as the RAG Ollama model" continuity rationale was moot since the CKB is rebuilt fresh. Both models are instantiated in Phase 2, not before. Core hybrid+RRF+rerank+not-found architecture was re-checked against 2026 SOTA and confirmed current.
4. **Stateless server.** No session state, no coref, no warm-start. Fresh internal state per call.
5. **Two install paths — demo + bring-your-own-corpus (Shape C, amended 2026-07-16).** (a) A prebuilt CKB ships as a release asset for a zero-build demo/proof over the 60-doc hard corpus; (b) bring-your-own-corpus — users install the `[corpus]` extra and run `scripts/build_corpus.py` against their own `manifest.yaml` to build a CKB over their own documents. The 60-doc corpus is the eval/proof fixture **and** the demo content, not the only supported content. This is an open-source project meant to be used by others, so BYO is first-class. Contextual Retrieval (Haiku) must be **optional/config-gated** (`contextualize.enabled`) so the BYO path works without an API key. Supersedes the earlier "ingest is dev-only" framing.
6. **Usability decisions (2026-07-16, with user).** Code license: **MIT**. Shipped index: **ship all chunk text with a fair-use + per-source attribution `NOTICE`** (keep the per-source `license` field so any source can be swapped/removed in one line if challenged; prefer CC-licensed equivalents where a swap is trivial). BYO corpus input: support **both** a `manifest.yaml` **and** a friction-free **local-folder** mode (`build_corpus.py --folder`) — local-file ingest is first-class; local files' `version` defaults to a content hash. Contextual Retrieval **defaults OFF when no API key is present** (build proceeds on raw text with a note); the shipped corpus is built with it ON.

## House rules (carried over from the RAG project)

- **YAML config over hard-coded constants** — all tunables live in `config/*.yaml`.
- **Deterministic, stable IDs** — chunk IDs are `sha256(canonical_url + version + chunk_index)` conceptually; the implementation (`src/kbmcp/models/ids.py`) joins fields with the ASCII unit-separator `\x1f` before hashing (prevents boundary-collision, e.g. `"ab"+"c"` vs `"a"+"bc"`) — never break this scheme, in concept or exact serialization (citations and eval gold labels depend on it). `config/corpus_config.yaml`'s `ids.scheme` field must stay in sync with this description.
- **Reproducible artifacts** — corpus builds, eval runs, and reports must be re-runnable and committed where they are proof artifacts (eval reports especially).
- **Never write to stdout in server code** — stdio transport; logs go to stderr.

## Environment & commands

- Windows 11, PowerShell. Venv at `venv\` — Python 3.11.9 (matches project pin `>=3.11,<3.13`).
- **Always invoke the venv interpreter directly** — `venv\Scripts\python.exe -m pytest ...` — because shell activation does not persist between Claude Code tool calls.
- Repo status: **Phase 0 complete.** Phase 1 (corpus construction) — **Milestones 1–3 complete and reviewed** (each: task reviews + whole-milestone review + independent adversarial review; all on `main`, not pushed):
  - **M1 Foundations & Data Model** — `[corpus]` extra, deterministic sha256 IDs (`models/ids.py`), 5-table SQLite schema + ops (`db/`), `corpus_config.yaml` loader, BYO manifest loader (`ingest/manifest.py`).
  - **M2 Fetch & Pin** — `ingest/fetch.py` (cache-first fetcher: generic httpx + arXiv/local recipes + **curl_cffi** TLS-impersonation for WAF hosts). `corpus/manifest.yaml` = **55 web-verified sources** across 4 domains (18 AI, 11 law-aireg, 12 law-contract, 14 fitness); raw bytes pinned to `corpus/raw/` (gitignored) with committed `*.meta.json` pin records. Notable routes: Europe PMC PDF for PMC sources, `curl_cffi` for Justia/nycourts/SEC.
  - **M3 Parse & Chunk** — `ingest/{parse,chunk,build,verify_structure}.py`: Docling `DocumentConverter` → **committed** `corpus/parsed/*.json` (44MB reproducibility anchor) → HybridChunker (Qwen3 tokenizer) → `ckb/ckb.sqlite` (gitignored). **55 docs / 2803 chunks**, 5-fixture structure gate passing, fully reproducible. `build.py`: `--force` rebuilds DB from committed parse, `--reparse` re-runs Docling. Torch forced eager (`TORCHDYNAMO_DISABLE=1` in `ingest/__init__.py`) so Docling PDF parse needs no C++ compiler.
  - Runtime models modernized 2026-07-23 (embedder Qwen3-Embedding-0.6B, reranker bge-reranker-v2-m3 — Decision 3). **82 tests passing.**
  - **M4 Contextual Retrieval — COMPLETE (code, review, and the generation run).** `ingest/context_windows.py` (pure, deterministic token-budgeted windows) + `ingest/contextualize.py` (pin store, Haiku 4.5 generator, `contexts_for_document`, cost estimator, `context_coverage` gate, CLI) + `build.py` wiring. Per-task reviews + whole-branch review (opus) all clean. **Generation run finished 2026-08-31: 2803/2803 chunks contextualized, 0 missing, 0 errors** (`corpus/contexts/*.json`, 55 files, 1.8MB, committed). Run across two sessions — 1759 pinned first, then the remaining 1044 — because the initial $5 of credit ran out mid-pass; the pin store made the resume free. **Actual total ~$6.7** against the original **$8.26** estimate. Re-verified end to end: `build --force --require-context` → docs=55 chunks=2803 contexts=2803 errors=0, zero NULL/empty contexts, structure gate 5/5, graph gate 3/3 active, **212 tests**.
    - **Estimator is optimistic by ~25%** (`--estimate` said $2.26 for the final 1044 chunks; actual $2.79). It charges one cache *write* per window and does not model re-writes when a window's 1h TTL lapses mid-run. Correct it before quoting a BYO-corpus build cost to users.
    - **Amendment to ingest spec §5 (2026-08-11, with user):** "section-scoped" is now **token-budgeted contiguous windows**. Measured: a bare `heading_path` section has a median size of **1 chunk** (1421 sections / 2795 chunks) and sits below Haiku 4.5's **4096-token** minimum cacheable prefix, so literal section-scoping would give no context and silently disable prompt caching. Windows pack **whole sections** to `window_target_tokens` (6000) and partition each document → 255 windows, 73% cacheable, 86% of chunks in a cacheable window. `contextualize.section_scoped: true` stays true and still means "never split a section" (except one over the hard cap).
    - **Known risk carried to M6:** `chunk.tokenizer` (`Qwen/Qwen3-Embedding-0.6B`) has **no revision pin**. Pins validate via `text_sha256` of chunk text, so an upstream tokenizer revision that shifts a chunk boundary would invalidate every pinned context at once and a keyless rebuild would silently produce a context-free CKB. Pin the revision when M6 finalizes `build_corpus.py`.
  - **Corpus correction 2026-08-11 (before any paid run, with user).** Two problems found while grounding M5: (a) `ucc-article-2` was CA Commercial Code **§2601 alone** — a 1.1k-char stub that never cites §1201, so the manifest's declared `references: [ucc-article-1]` had **no textual basis** and the Phase-1 T4 exit criterion was unreachable by any correct resolver. Both UCC sources widened to their **Division/Chapter 2** (`codes_displayText.xhtml`), giving the real resolvable edge **§2202 → "(Section 1303)" → ucc-article-1 §1303**; ucc-article-2 grew 1.1k→11.8k chars and gained the Statute of Frauds. (b) 5 chunks of publisher site chrome now dropped by `chunk.is_boilerplate` (`chunk.drop_boilerplate`) — **narrow per-publisher signatures only**: a short-line-ratio heuristic was measured and rejected because 31 of 2795 chunks exceed 70% short lines and nearly all are real content (arXiv LaTeX, author blocks, Wikipedia pseudocode). Also added manifest reconciliation to `build_ckb` (`stats["pruned"]`): a URL/version change used to orphan the old `doc_id` forever. Structure gate re-derived, 5/5.
  - **M5 Cross-Reference Resolution Graph — complete and reviewed.** `ingest/citations.py` (deterministic extraction; anchors come from BOTH standalone header lines in text (EUR-Lex) and `heading_path`'s last element (leginfo) — neither covers the corpus alone) + `ingest/graph.py` (corpus-wide anchor index → manifest-scoped resolution → `references`/`adjacent` edges, re-runnable/idempotent CLI) + `ingest/verify_graph.py` (the ground-truth gate, mirroring `verify_structure.py`) + `db/ops.py` edge ops (injective json-payload `edge_id`, idempotent insert). Full run measured: **chunks_scanned=2803** (127 carry an anchor, 4.5%), **adjacent=2748**, **references_resolved=737**, **unresolved=1388**, **ambiguous=0**, **self_citations=22**, **errors=0**; 4873 edges total (adjacent 2748 + references 2125). All three expectations pass (104, 92, and 1 edges); `all_passed` True. **212 tests.**
    - **Whole-branch review caught what a per-task review could not:** 58 of 196 cross-document edges (29%) pointed at the chunk *before* the cited article's body, because the chunker routinely ends a chunk with the NEXT article's header. Fixed via `graph.min_anchor_body_chars` (200) — a text-derived header with less body than that defers the anchor to the next chunk of the same document. Now **0/196**. Note the earlier "725/725 targets contain the cited header" audit was true but insufficient: it never checked that the article's *body* followed.
    - **Cross-milestone defect found and fixed:** once edges existed, `build._delete_doc` could not delete chunks (`edges.from_chunk`/`to_chunk` FK, `PRAGMA foreign_keys = ON`), so `build --force` — the command that applies context pins — aborted. `_delete_doc` now clears both edge sides first.
    - **arXiv/DOI resolve via document-level anchors** derived from manifest URLs, scope-exempt because the identifiers are globally unique; section-level anchors stay manifest-scoped. Before this the 18-document AI domain had zero cross-document edges.
    - **`parent` edges deliberately not built**: `heading_path` gives no usable hierarchy here (empty on EUR-Lex, a constant breadcrumb on leginfo).
    - **The formerly-pending UCC expectation now resolves (2026-08-31).** `ucc-article-1`'s manifest URL was repointed from CA Commercial Code Division 1 **Chapter 2** (§§1201–1206) to **Chapter 3**, where the §1303 that `ucc-article-2` §2202 actually cites lives, then re-fetched and re-parsed. All **3** graph expectations are now ACTIVE and passing — the Phase-1 T4 exit criterion is met by a real textual edge, not a weakened assertion. `verify_graph` still reports `pending` entries and excludes them from `all_passed`; none remain.
  - **M6 BM25 index — BM25 half complete (in progress; `build_corpus.py` remains).** `index/bm25_store.py`: SQLite **FTS5** standalone (not external-content) index built directly inside `ckb.sqlite` (`BM25Store.build/query/is_stale`), superseding the planned `rank-bm25` dependency entirely — per-column weighting over `context` and `text` (weights 1.0/2.0; `heading_path` deliberately excluded as noise, per the design spec — empty on EUR-Lex, a constant site breadcrumb on leginfo), `to_match_query` escaping untrusted query text against FTS5 syntax injection, and a `bm25_meta` table holding a sha256 fingerprint over `chunk_id + context + text` (content-covering, so an M4 context-only edit is detected, not just ID changes) for `is_stale()` freshness checks. Rewrite was motivated by measured defects in the hand-rolled approach it replaced: a stemmer that failed 7 of 9 legal singular/plural pairs, a `.lower().split()` tokenizer that turned `Article 22(1)` into the single token `22(1)`, and a stopword list that dropped `not` from 200 chunks containing "shall not"/"may not". **Verified against the live 2803-chunk CKB (2026-09-02):** build reports `indexed 2803 chunks`; `is_stale()` False on a clean index and correctly flips True/False around a mutate/rollback probe; all four smoke queries (including `Article 22`, `shall not`) return hits; **98** chunks are reachable only via their generated context and not their own text for "GDPR" — direct evidence of M4's retrieval-reach payoff. `rank-bm25` removed from the `[corpus]` extra (grepped first: never imported). **246 tests passing.**
    - **Remaining for M6:** `build_corpus.py` (BYO-corpus entry point), which must carry the M4-noted follow-up of pinning the `Qwen/Qwen3-Embedding-0.6B` tokenizer revision (currently unpinned — see M4 known-risk note above).
    - **Two design items deliberately deferred to Phase 2 (whole-branch review, 2026-09-02), recorded here since the working notes that held them get deleted.** (a) `is_stale()` folds `weights_json` into its comparison, but weights are a query-time `bm25()` argument that cannot invalidate the stored index — on a shipped read-only CKB a weights change alone raises a false "stale" the user cannot clear, since they cannot rebuild. Split `is_stale()` (digest/tokenize/schema_version) from a separate `config_differs()`, keeping weights recorded as provenance only. (b) `to_match_query()` OR-joins every token, so no caller can express a phrase — the spec validated phrase behaviour (`"shall not be subject"`) but the public API cannot reach it, costing precision on the exact-fact eval tier. Preserving user-quoted spans as FTS5 phrases stays injection-safe by the same argument (`\w` cannot close a quote).
  - **Next: finish M6 `build_corpus.py`.** Remaining after: benchmark + gold resolution (M7).
- Working conventions:
  - Install: `venv\Scripts\python.exe -m pip install -e ".[dev]"`
  - Tests: `venv\Scripts\python.exe -m pytest tests\test_x.py::test_name -v`
  - Toy MCP server (Phase 0 reference, not production): `examples\toy_server.py`, registered with Claude Code as `kb-toy`
- Planned conventions (not yet implemented — see master plan for phases):
  - Package layout: `src/kbmcp/`; console entry point `kbmcp`
  - Corpus build (dev-only): `python scripts/build_corpus.py`
  - Eval: `python -m kbmcp.eval run` → per-tier report in `eval/runs/<ts>/report.md`
